---
title: Two of the four, running
reveal: replace
notes: |
  DEMO SLIDE — 3 min, hard stop. `reveal: replace`, so each step swaps the
  demo rather than stacking it; arrow DOWN between them.
  Two demos, not four. "Don't redraw what did not move" and "don't send it
  twice" were both on the showcase slide already — the flow graph pans and
  zooms over a subtree scale, and the GL scene is one CallList a frame — so
  point back at slide 19 instead of showing them twice.
  1 — the panel is this deck's own commit history, which is a few dozen
      points: the right size for a slide and the wrong size for the claim.
      So press the button. That opens the components package's own charts
      example in ITS OWN window: a million-point series, a streaming section
      appending 60 points a second, small multiples at 90px, and a HUD under
      each chart printing what the last frame cost — mode, span, command
      count, wire bytes. Zoom in and out of the million and read the byte
      count out loud: it does not move. Cost follows pixels, not points.
      Scroll a chart out of view and its frame counter freezes.
  2 — the flood. Say that the output is correct either way and the only
      question is how many frames you spent on it.
  Stop both before moving on.
---

# Don't draw detail nobody can see

<Charts height={260} revealAt={0} />

<ChartsDemo />

^^^

# Don't draw too often

<Terminal
  height={380}
  command={["bash", "-lc", "yes 'the wire carries drawing, not pixels' | head -20000"]}
/>
